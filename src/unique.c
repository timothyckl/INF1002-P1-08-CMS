#include "unique.h"

// TODO: implement helper(s) for the unique feature once defined
// - accept StudentDatabase * and any required parameters
// - compute or transform data (e.g., summary stats / filtered set / export
//   buffer)
// - provide a clear status/result structure for CLI to display

//   if (!db_loaded) { print "open first"; return; }
//   // any prompts for extra input
//   compute_result_from_tables(db)
//   print formatted summary/whatever the feature is
